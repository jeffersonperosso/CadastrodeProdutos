# 📦 Sistema de Cadastro de Produtos (PHP + JS Vanilla)

Este é um projeto de Cadastro de Produtos, desenvolvido sem frameworks, utilizando:

- PHP (vanilla) para a API
- JavaScript para consumir os endpoints
- HTML + CSS para interface
- JSON como formato de salvar os dados.

 ▶ Como Rodar o Projeto

### 1. Abra o terminal na pasta do projeto e rode o servidor embutido do PHP:

```php -S localhost:8000 -t public```

Abra o navegador em:

```http://localhost:8000/public/index.html```

Pronto! Sistema pronto para usar.
